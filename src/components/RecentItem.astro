<div class="recent-items">
    <slot/>
</div>

<style>
    :global(.recent-item-img) {
      width: 100%;
      height: 100px;
      object-fit: cover;
    }

    :global(.recent-item-body) {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    :global(.recent-item-title) {
      text-decoration: none;
      font-weight: 500;
    }

    :global(.recent-item-date) {
      font-size: 14px;
      color: var(--text-color2);
    }

    :global(.recent-item) {
      border-bottom: dashed 1px #dddddd;
      padding: 12px 0;
    }
</style>